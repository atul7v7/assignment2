//  here we will get the data

import React from "react";
import Element from "../components/Element";

const ElementContainer = ({ data }) => {
  return (
    // question section
    // videos section to no od video
    // mis sect
    <div className="element_container">
      <diV className="q&n">
        <Element img={"at"} content={"at"} file={"at"} />
      </diV>
      <div className="video_block">{}</div>
      <div className="mis_block">
        <Element img={"at"} content={"at"} file={"at"} />
      </div>
    </div>
  );
};
